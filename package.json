{
  "name": "natilon-starter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Astro site + Natilon CMS admin, deployed as one Cloudflare Worker.",
  "scripts": {
    "dev": "node --env-file-if-exists=.env node_modules/.bin/astro dev",
    "build": "astro build && node scripts/postbuild.mjs",
    "preview": "npm run build && wrangler dev -c worker/wrangler.jsonc",
    "deploy": "npm run build && wrangler deploy -c worker/wrangler.jsonc"
  },
  "dependencies": {
    "@natilon/admin-ui": "^0.18.0",
    "@natilon/astro-blocks": "^0.7.0",
    "@natilon/astro-cms": "^5.1.1",
    "@natilon/blocks": "^0.4.0",
    "@natilon/cms-server": "^0.24.1",
    "@natilon/cms-worker": "^0.6.1",
    "astro": "^7.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
